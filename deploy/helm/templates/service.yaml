apiVersion: v1
kind: Service
metadata:
  name: {{ include "allinkl-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "allinkl-webhook.name" . }}
    chart: {{ include "allinkl-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "allinkl-webhook.name" . }}
    release: {{ .Release.Name }}
